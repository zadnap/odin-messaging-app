import styles from './ConversationList.module.scss';

const ConversationList = () => {
  return (
    <section className={styles.conversationList}>Conversation List</section>
  );
};

export default ConversationList;
